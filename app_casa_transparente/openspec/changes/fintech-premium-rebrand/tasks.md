# Implementation Tasks

- [x] 1. Configurar dependências e globais
  - Verificar presenças de `google_fonts` e `phosphor_flutter` no pubspec.yaml.
  - Importar pacotes em arquivos necessários (main.dart e views).
- [x] 2. `main.dart` (Theming e Header/Navegador)
  - Alterar ThemeData principal usando `GoogleFonts.interTextTheme(...)`
  - Substituir o ícone superior esquerdo (hoje `Icons.home_outlined`) por `PhosphorIcons.bank(PhosphorIconsStyle.regular)`.
  - Refatorar a base do `_navBtn` para receber explicitamente um `PhosphorIconData` regular e outro fill, trocando a lógica de preenchimento.
  - Substituir `receipt_long`, `credit_card` e `analytics` para equivalentes no Phosphor em estilo regular e fill.
  - Substituir ícones de Search (`Icons.search`) e de Hoje (`Icons.today`).
  - Substituir navigation icons na navegação de mês (`Icons.chevron_left` e `right`).
- [x] 3. Atualizar Views `finance/views`
  - Em `DespesasTab.dart`:
    - Atualizar FAB para `PhosphorIcons.plus(PhosphorIconsStyle.bold)`.
    - Trocar ícones de pago `Icons.check_circle` e cancelar/pendente `Icons.cancel`.
    - Trocar ícones das actions do bottomsheet (`edit_outlined`, `delete_outline`).
  - Em `ResumoTab.dart`:
    - Trocar `Icons.savings_outlined` por `PhosphorIcons.piggyBank(PhosphorIconsStyle.regular)`.
    - Trocar `Icons.info_outline`.
- [x] 4. Atualizar Views `cartao/views`
  - Em `CartaoTab.dart`:
    - Atualizar FAB para `PhosphorIcons.plus(PhosphorIconsStyle.bold)`.
    - Trocar `check_circle`, `restaurant` (`PhosphorIcons.forkKnife(...)`), `undo` e `check_circle_outline`.
    - Trocar ações de editar e excluir dentro do bloco expansível.
- [x] 5. Atualizar Shared Widgets e BottomSheets
  - `PersonSummaryRow.dart`: Atualizar `Icons.check_circle` e `Icons.error` (`PhosphorIcons.warningCircle`).
  - `add_expense_sheet.dart` e `add_purchase_sheet.dart`: Checar presença de `Icons.calendar_today`, `Icons.close`, `Icons.save` e substituir por equivalentes.
